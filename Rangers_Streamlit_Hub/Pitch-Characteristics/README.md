# Pitch Similarity App v45

Run:

```bash
python3 -m pip install -r requirements.txt
python3 -m streamlit run app.py
```

Headshots are now cached locally in `headshot_cache/` after the first successful fetch or manual upload.
